name: Deploy client to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install client dependencies
        run: npm ci --prefix client

      - name: Note backend URL source
        run: |
          if [ -z "${{ vars.VITE_SERVER_URL }}" ]; then
            echo "::warning::VITE_SERVER_URL repo variable not set — build will use the URL baked into client/src/config.js."
          else
            echo "Using VITE_SERVER_URL repo variable (overrides client/src/config.js)."
          fi

      - name: Build client
        run: npm run build --prefix client
        env:
          VITE_BASE_PATH: /${{ github.event.repository.name }}/
          VITE_API_URL: ${{ vars.VITE_SERVER_URL != '' && format('{0}/api', vars.VITE_SERVER_URL) || '' }}
          VITE_SERVER_URL: ${{ vars.VITE_SERVER_URL }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: client/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4